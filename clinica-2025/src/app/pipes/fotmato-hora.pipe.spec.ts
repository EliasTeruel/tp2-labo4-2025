import { FotmatoHoraPipe } from './fotmato-hora.pipe';

describe('FotmatoHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FotmatoHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
