import { FotmatoFechaPipe } from './fotmato-fecha.pipe';

describe('FotmatoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FotmatoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
