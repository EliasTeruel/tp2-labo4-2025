import { ResaltarPacientesDirective } from './resaltar-pacientes.directive';

describe('ResaltarPacientesDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarPacientesDirective();
    expect(directive).toBeTruthy();
  });
});
