import { AvatarErrorDirective } from './avatar-error.directive';

describe('AvatarErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarErrorDirective();
    expect(directive).toBeTruthy();
  });
});
