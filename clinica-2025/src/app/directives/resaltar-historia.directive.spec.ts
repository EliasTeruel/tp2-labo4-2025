import { ResaltarHistoriaDirective } from './resaltar-historia.directive';

describe('ResaltarHistoriaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarHistoriaDirective();
    expect(directive).toBeTruthy();
  });
});
