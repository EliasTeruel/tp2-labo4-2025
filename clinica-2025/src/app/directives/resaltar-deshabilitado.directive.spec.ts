import { ResaltarDeshabilitadoDirective } from './resaltar-deshabilitado.directive';

describe('ResaltarDeshabilitadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarDeshabilitadoDirective();
    expect(directive).toBeTruthy();
  });
});
