import { ResaltarRealizadoDirective } from './resaltar-realizado.directive';

describe('ResaltarRealizadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarRealizadoDirective();
    expect(directive).toBeTruthy();
  });
});
